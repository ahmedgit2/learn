import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  containerStyle: {
    flexDirection: 'row',
    margin: 5,
    alignItems: 'center',
  },
  textStyle: {
    marginHorizontal: 3,
    color: '#588',
    fontSize: 14,
  },
});
